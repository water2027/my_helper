version: '3.8'

services:
    backend:
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env
        environment:
            GIN_MODE: release
            TZ: Asia/Shanghai
